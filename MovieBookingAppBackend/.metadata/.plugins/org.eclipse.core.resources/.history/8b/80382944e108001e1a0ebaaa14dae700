management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: * 
  endpoints:
    health:
      show-details: always
    metrics:
      enabled: true
    prometheus:
      enabled: true
